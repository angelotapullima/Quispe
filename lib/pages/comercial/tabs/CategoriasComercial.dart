

import 'package:flutter/material.dart';


class CategoriasComercial extends StatelessWidget {
  const CategoriasComercial({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Categorias'),)
    );
  }
}