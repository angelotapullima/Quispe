

import 'package:flutter/material.dart';


class CategoriasFerroPage extends StatelessWidget {
  const CategoriasFerroPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Categorias'),)
    );
  }
}